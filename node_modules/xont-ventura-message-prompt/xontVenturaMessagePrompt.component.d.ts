import { EventEmitter } from '@angular/core';
import { Http } from "@angular/http";
import { Location } from '@angular/common';
import { MessageService } from './message.service';
import { Subscription } from 'rxjs';
export declare class XontVenturaMessagePromptComponent {
    private http;
    private location;
    private messageService;
    constructor(http: Http, location: Location, messageService: MessageService);
    message: {};
    backgroundColor: string;
    title: string;
    messageText: string;
    okButtonText: string;
    cancelButtonText: string;
    id?: string;
    messageType?: string;
    onOK: EventEmitter<any>;
    onCancel: EventEmitter<any>;
    busy: Subscription;
    show(object: any, taskCode: string): void;
    showConfirm(messageText: string, okButtonText: string, cancelButtonText: string): void;
    private confirm_ok();
    private confirm_cancel();
    private alert_ok();
    showAlert(messageText: string, okButtonText: string): void;
    confirmation(msgID: number): void;
    confirmation(msgID: number, para1: string, para2: string, para3: string, para4: string, para5: string, para6: string): void;
    confirmation(msgID: number, para1: string, para2: string, para3: string, para4: string, para5: string, para6: string, yesButtonText: string, noButtonText: string): void;
    alert(msgID: number, para1: string, para2: string, para3: string, para4: string, para5: string, para6: string): void;
}
