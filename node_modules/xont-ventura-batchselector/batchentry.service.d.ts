import 'rxjs/add/operator/map';
import 'rxjs/add/operator/catch';
import { Http } from "@angular/http";
import { CommonService, DatetimeService } from "xont-ventura-services";
export declare class BatchEntryService {
    private http;
    private commonService;
    private datetimeService;
    constructor(http: Http, commonService: CommonService, datetimeService: DatetimeService);
    private taskCode;
    private getAPIPrefix();
    GetProduct(taskCode: string, productCode: string): any;
    HandleExistingBatchNo(taskCode: string, territoryCode: string, warehouseCode: string, locationCode: string, productCode: string, batchNo: string, unitCost: number): any;
}
