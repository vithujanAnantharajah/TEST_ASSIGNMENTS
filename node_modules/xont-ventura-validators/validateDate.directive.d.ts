import { AbstractControl, FormControl } from '@angular/forms';
import { Http } from "@angular/http";
export declare class ValidateDateDirective {
    private http;
    validator: Function;
    format: string;
    constructor(http: Http);
    validate(c: FormControl): any;
    validateDateFactory(): (c: FormControl) => {
        validateDate: {
            valid: boolean;
        };
    };
    validateYearMonthDate(year: any, month: any, date: any): {
        validateDate: {
            valid: boolean;
        };
    };
}
export interface Validator {
    validate(c: AbstractControl): {
        [key: string]: any;
    };
}
