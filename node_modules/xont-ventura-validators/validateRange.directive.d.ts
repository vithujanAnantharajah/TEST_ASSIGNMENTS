import { Validator, AbstractControl } from '@angular/forms';
export declare class ValidateRangeDirective implements Validator {
    validateRange: string;
    constructor(validateRange: string);
    validate(c: AbstractControl): {
        [key: string]: any;
    };
}
