import { EventEmitter } from "@angular/core";
export declare class XontVenturaMultiplePromptComponent {
    private data;
    private dataBK;
    private backupData;
    private rowsOnPage;
    private sortBy;
    private columnDefs;
    private title;
    private promptFilter;
    private type;
    private checkedAll;
    private message;
    id: string;
    onSelect: EventEmitter<any>;
    onCancel: EventEmitter<string>;
    private ngOnInit();
    show(gridOption: any): void;
    private changeType(type);
    private unselectAll();
    private selectAll();
    private onKey(event);
    private cancel();
    private ok();
    private selectRow(item);
    private chkSelectAll_Change();
}
