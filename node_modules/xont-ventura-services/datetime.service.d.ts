import { Http } from '@angular/http';
import 'rxjs/add/operator/map';
export declare class DatetimeService {
    private http;
    format: string;
    constructor(http: Http);
    getDisplayDate(date: any): string;
    isValidDateString(text: any): boolean;
    private validateYearMonthDate(year, month, date);
    getDateTimeForString(text: string): any;
    getClientDateFormat(): string;
    get12HourTime(date: Date): string;
    get24HourTime(date: Date): string;
    validateFromToDates(fromDate: string, toDate: string): boolean;
}
